apiVersion: v1
kind: Service
metadata:
  name: test-dns-lb
  namespace: default
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test-dns-lb.k8s.local
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
  selector:
    app: test-dns
